import {Button} from 'react-bootstrap'
import {logIn} from './../../../constants'

const NavLeft = () => {
    return (
        <div>
            <Button>{logIn}</Button>
        </div>
    )
}

export default NavLeft